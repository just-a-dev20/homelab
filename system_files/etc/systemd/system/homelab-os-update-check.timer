[Unit]
Description=Daily Homelab OS update check (bootc, WebUI bridge)

[Timer]
# Daily plus a delayed first run after boot; catch up if the machine was off.
OnCalendar=daily
OnBootSec=15min
Persistent=true
RandomizedDelaySec=30min

[Install]
WantedBy=timers.target
